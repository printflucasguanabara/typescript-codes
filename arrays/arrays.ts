let numbers: number[] = [1, 2, 3, 4, 5];

// ---

console.log(numbers[2]);

let names: string[] = [ 'Goku', 'Vegeta', 'Trunks'];

console.log(names[0]);

let resultSoma = numbers[0] + numbers[3]; //5

console.log(resultSoma)