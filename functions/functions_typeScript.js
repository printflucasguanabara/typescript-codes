var x = 10;
var y = 5;
var result = x + y;
console.log(result); // Exibe o resultado

// Declara uma função
function addNumbers(a, b) {
    return a + b;
}

// Usa a função
var resultAdd = addNumbers(10, 12);
console.log(resultAdd); // Exibe o resultado da adição

// Exemplos de funções de string
function greetings(personName) {
    console.log(`Olá ${personName}, como você está?`);
}

greetings("lucas");
